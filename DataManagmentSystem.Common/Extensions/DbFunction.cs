using System;

namespace DataManagmentSystem.Common.Extensions {
    public static class DbFunction {
        public static decimal GetDistance(decimal lng1, decimal lat1, decimal lng2, decimal lat2)
            => throw new NotSupportedException();
    }
}
